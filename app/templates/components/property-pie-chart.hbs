{{high-charts chartOptions=chartOptions content=normalizedChartData}}
{{#power-select 
    options=sortedModelProps 
    selected=selectedModelProperty
    onchange=(action "initChart") 
    as |name|
}}
  {{name}}
{{/power-select}}
