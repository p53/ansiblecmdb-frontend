<div class="row">
    <div class="col-xs-7 col-sm-3 col-md-3 col-xm-offset-2 col-sm-offset-3 col-md-offset-2">
        {{#if user.isSaving }}<div class="sk-rotating-plane"></div>{{/if}}
        <form>
            {{#if error}}
                <div class="alert alert-danger" role="alert">{{ error }}</div>
            {{/if}}
            <div class="form-group">
                <label>Password</label>
                {{input type="password" class="form-control" value=(mut password)}}
                <label>Repeat Password</label>
                {{input type="password" class="form-control" value=(mut repeatPassword)}}
            </div>
            <button type="button" class="btn btn-primary" {{ action 'updateUser' }}>Apply</button>
        </form>
    </div>
</div>
